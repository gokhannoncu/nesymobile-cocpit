node_modules
.turbo
.pnpm-store
dist
.next
out
build
coverage
*.tsbuildinfo
.env
!.env.example
!apps/api/.env
.env.local
.env.production
*.env.local
*.log
.DS_Store
__pycache__
*.pyc
.venv

# Presentation Studio local data
.data

# graphify — local knowledge graph output (not pushed to remote)
graphify-out/

# Cursor hook local state (pending prod rebuilds / logs)
.cursor/hooks-state/

# Brainstorm visual companion (local only)
.superpowers/

# Workflow run artifacts (screenshots/video)
apps/api/maestro/

# Automation benchmark/scratch artifacts (Claude perf session)
apps/api/_*
