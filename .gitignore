node_modules
.turbo
.pnpm-store
dist
.next
out
build
coverage
*.tsbuildinfo
.env
!.env.example
!apps/api/.env
.env.local
.env.production
*.env.local
*.log
.DS_Store
__pycache__
*.pyc
.venv

# Presentation Studio local data
.data

# graphify — local knowledge graph output (not pushed to remote)
graphify-out/

# Windows LAN IP for Mac PAC (local only)
.nesy-lan-proxy.host

# Cursor hook local state (pending prod rebuilds / logs)
.cursor/hooks-state/

# Brainstorm visual companion (local only)
.superpowers/

# Workflow run artifacts (screenshots/video)
apps/api/maestro/

# Automation benchmark/scratch artifacts (Claude perf session)
apps/api/_*

# Device-measurement scratch: one-off bridge probes, tree dumps, run JSON.
# Transient by design — a finding worth keeping goes in the phase RUN_PLAY, not
# here. RUN_PLAY once pointed at a script in this directory that had already
# been deleted; anything a future run needs belongs in scripts/ instead.
scratchpad/

# Local opt-in performance traces (large, device-specific diagnostic artifacts).
artifacts/live-profile/
