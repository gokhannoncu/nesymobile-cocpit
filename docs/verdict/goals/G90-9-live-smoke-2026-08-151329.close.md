# G90.9 live-qualified — 2026-08-15 16:29 +03

Fresh prod lineage on reference commit `3770d2a`. Old D30 `pnpm prod` was
not reused. This is metadata-contract qualification, not BD.3 and not a
D60 campaign start.

## Identity

```text
gitCommit            = 3770d2af4eca0cc170560562130b6632d635ad2a
g90_9ImplementationClean = true
workingTreeClean     = false   # only scripts/g90-9-live-smoke.mjs + verdict-run flags
apiCommand           = node dist/server.js
apiPid               = 55798
apiStartedAt         = Sat Aug 15 16:27:28 2026
packVersion          = nesy.courier@1.30.0
dbSchema             = up-to-date
deviceId             = R6CW400BC8N
```

## Smoke A — uninjected compatibility

`run_c0f74f73-a919-40ce-83ac-7e8f2281701c`

```text
injectedFault   = null
productVerdict  = PASS_ONLINE
observedClass   = null
cleanup         = SUCCEEDED
```

Business result lives on `productVerdict`. Uninjected `observedClass` stays
`null` so D30/D90 rows do not enter the D60 matrix.

## Smoke B — injected metadata contract

`run_5a503b8d-88a5-4fc3-a9e1-33508fc85be4`

```text
injectedFault   persist/readback = BACKEND_TIMEOUT
expectedClass   derived          = BACKEND_TIMEOUT
observedClass                    = null
productVerdict                   = PASS_ONLINE
cleanup                          = SUCCEEDED
```

`BACKEND_TIMEOUT` is the same token on both axes on purpose. The field is
the question. Login still passed because no injector ran. `observedClass`
was not copied from the input.

## Close

G90.9 is **LIVE_QUALIFIED**. NEXT = G90.10 (six semantic injectors).
Formal D60 campaign remains `NOT_STARTED` (13 Sep–12 Oct 2026).
