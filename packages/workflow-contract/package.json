{
  "name": "@nesy/workflow-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Verdict WorkflowIR v2 SÖZLEŞMESİ — generic step union, typed Condition AST, Continue Gate/Final Oracle policy, occurrence correlation, orthogonal outcome axes, allowlisted REMOTE_ACTION. TAŞIMA/DB BİLGİSİ YOK. DOMAIN BİLGİSİ YOK: STOP/PARCEL/TOUR/COURIER/MATCH/BETTING buraya giremez (D.5, D.6, D.6A).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "devDependencies": {
    "@nesy/eslint-config": "workspace:*",
    "@nesy/typescript-config": "workspace:*",
    "@types/node": "^22.10.10",
    "eslint": "^9.39.1",
    "typescript": "^5.8.3",
    "vitest": "^3.0.5"
  }
}
