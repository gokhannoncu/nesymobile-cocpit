{
  "_fixtureNote": "Defect: a fixed wait smuggled in as a step field. Expected issue code FIXED_WAIT_FORBIDDEN.",
  "schemaVersion": 2,
  "workflowId": "fixture-invalid-fixed-wait",
  "workflowVersion": 1,
  "name": "Fixed wait must be rejected",
  "source": { "kind": "FIXTURE", "ref": "invalid-fixed-wait" },
  "inputs": [],
  "variables": [],
  "entryStepId": "s1",
  "policies": {
    "runDeadlineMs": 60000,
    "cleanupDeadlineMs": 10000,
    "defaultRetry": { "maxAttempts": 1, "effectClass": "READ_ONLY" },
    "artifactPolicy": { "captureOnSuccess": false, "captureOnFailure": true },
    "redactionPolicy": {}
  },
  "capabilityRequirements": [],
  "sourceMap": [{ "ref": "sm1", "planStepId": "s1" }],
  "steps": [
    {
      "planStepId": "s1",
      "kind": "NOOP",
      "sourceMapRef": "sm1",
      "timeoutMs": 1000,
      "sleepMs": 15000,
      "retryPolicy": { "maxAttempts": 1, "effectClass": "READ_ONLY" },
      "capabilityRequirements": [],
      "next": null
    }
  ]
}
