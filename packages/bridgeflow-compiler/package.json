{
  "name": "@nesy/bridgeflow-compiler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Verdict BridgeFlowCompiler — Domain Pack semantic macro/action/test profile → generic WorkflowIR v2 → deterministic BridgeFlowPlan + UiWaitPlan compilation. EXECUTOR YOK: plan üretir, çalıştırmaz. CİHAZ ACTION YOK: Bridge command dispatch etmez. DOMAIN BUSINESS TYPE YOK: STOP/PARCEL/TOUR/COURIER export edemez. Bu sözleşmeler test-bound'dur.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "dependencies": {
    "@nesy/workflow-contract": "workspace:*",
    "@nesy/domain-pack-contracts": "workspace:*",
    "@nesy/bridge-contract": "workspace:*"
  },
  "devDependencies": {
    "@nesy/eslint-config": "workspace:*",
    "@nesy/typescript-config": "workspace:*",
    "@types/node": "^22.10.10",
    "eslint": "^9.39.1",
    "typescript": "^5.8.3",
    "vitest": "^3.0.5"
  }
}
