{
  "name": "@nesy/oracle-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Verdict Phase 5 Oracle Engine v2: Continue Gate and Final Oracle evaluators over normalized evidence facts.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "dependencies": {
    "@nesy/execution-contract": "workspace:*",
    "@nesy/workflow-contract": "workspace:*"
  },
  "devDependencies": {
    "@nesy/eslint-config": "workspace:*",
    "@nesy/typescript-config": "workspace:*",
    "@types/node": "^22.10.10",
    "eslint": "^9.39.1",
    "typescript": "^5.8.3",
    "vitest": "^3.0.5"
  }
}
