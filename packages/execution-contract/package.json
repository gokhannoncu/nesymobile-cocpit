{
  "name": "@nesy/execution-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Verdict Phase 5 execution/runtime contracts: run manifest, occurrences, action lifecycle, evidence journey, queue and broker axes. No domain business commands.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "dependencies": {
    "@nesy/bridge-contract": "workspace:*",
    "@nesy/bridgeflow-compiler": "workspace:*",
    "@nesy/control-contract": "workspace:*",
    "@nesy/workflow-contract": "workspace:*"
  },
  "devDependencies": {
    "@nesy/eslint-config": "workspace:*",
    "@nesy/typescript-config": "workspace:*",
    "@types/node": "^22.10.10",
    "eslint": "^9.39.1",
    "typescript": "^5.8.3",
    "vitest": "^3.0.5"
  }
}
